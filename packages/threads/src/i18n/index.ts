// Locale-strings sub-export for @pelilauta/threads.
//
// Stage 1 scaffold — empty until Stage 2 populates `fi` and `en` with the
// initial owned key set (threads:card.inChannel, threads:frontpage.showMore,
// threads:frontpage.error.fetchFailed). No runtime code, no side effects —
// the only non-data import permitted here is the `NestedTranslation` type
// from `@pelilauta/i18n`.

export {};
