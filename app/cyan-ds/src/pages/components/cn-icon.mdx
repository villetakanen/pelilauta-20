---
title: CnIcon Component
layout: ../../layouts/Book.astro
---
import CnIcon from "@cyan/components/CnIcon.astro";
import { icons as community } from "@pelilauta/icons";
import { icons as managed } from "@myrrys/proprietary-icons";
import { FallbackIcons } from "@cyan/components/CnIconFallback";

# CnIcon Component

The `CnIcon` component is the high-fidelity, source-code-first icon primitive for the Cyan Design System. It resolves icons across three tiers of registries and inlines them via SSR.

## Sizing Matrix

<div style="display: flex; align-items: center; gap: 2rem; flex-wrap: wrap; background: var(--cn-surface-1); padding: 2rem; border-radius: var(--cn-border-radius-medium);">
  <div style="text-align: center;">
    <CnIcon noun="fox" size="xsmall" />
    <div style="font-size: 0.75rem; margin-top: 0.5rem;">xsmall</div>
  </div>
  <div style="text-align: center;">
    <CnIcon noun="fox" size="small" />
    <div style="font-size: 0.75rem; margin-top: 0.5rem;">small</div>
  </div>
  <div style="text-align: center;">
    <CnIcon noun="fox" size="medium" />
    <div style="font-size: 0.75rem; margin-top: 0.5rem;">medium</div>
  </div>
  <div style="text-align: center;">
    <CnIcon noun="fox" size="large" />
    <div style="font-size: 0.75rem; margin-top: 0.5rem;">large</div>
  </div>
  <div style="text-align: center;">
    <CnIcon noun="fox" size="xlarge" />
    <div style="font-size: 0.75rem; margin-top: 0.5rem;">xlarge</div>
  </div>
</div>

## Grid Alignment

Verifying 8px baseline grid centering:

<div style="padding: 2rem; background: var(--cn-surface-2); display: inline-block; position: relative; border-radius: var(--cn-border-radius-medium);">
  <div style="position: absolute; inset: 0; background-image: radial-gradient(var(--cn-surface-4) 1px, transparent 1px); background-size: 8px 8px; opacity: 0.5;"></div>
  <div style="position: relative; background: white; border: 1px solid var(--cn-surface-4);">
    <CnIcon noun="add" size="small" />
  </div>
</div>

## Thematic Branding (Tier 1)

Automatic mode-aware palette shifting for the `mekanismi` icon:

<div style="display: flex; gap: 2rem; padding: 2rem; background: var(--cn-surface-3); border-radius: var(--cn-border-radius-medium);">
  <div style="text-align: center;">
    <CnIcon noun="mekanismi" size="large" />
    <div style="font-size: 0.75rem;">Dynamic Palette</div>
  </div>
</div>

## Tiered Icon Galleries

### Community MIT (Tier 1)
<div style="display: grid; grid-template-columns: repeat(auto-fill, minmax(100px, 1fr)); gap: 1rem; margin-top: 1rem;">
  {Object.keys(community).map((noun) => (
    <div style="display: flex; flex-direction: column; align-items: center; padding: 1rem; background: var(--cn-surface-2); border-radius: 8px;">
      <CnIcon noun={noun} size="small" />
      <span style="font-size: 0.7rem; margin-top: 0.5rem; word-break: break-all;">{noun}</span>
    </div>
  ))}
</div>

### Managed Assets (Tier 2)
<div style="display: grid; grid-template-columns: repeat(auto-fill, minmax(100px, 1fr)); gap: 1rem; margin-top: 1rem;">
  {Object.keys(managed).map((noun) => (
    <div style="display: flex; flex-direction: column; align-items: center; padding: 1rem; background: var(--cn-surface-2); border-radius: 8px;">
      <CnIcon noun={noun} size="small" />
      <span style="font-size: 0.7rem; margin-top: 0.5rem; word-break: break-all;">{noun}</span>
    </div>
  ))}
  {Object.keys(managed).length === 0 && <p style="font-size: 0.8rem; grid-column: 1/-1; opacity: 0.6;">No managed assets registered.</p>}
</div>

### Core Fallbacks (Tier 3)
<div style="display: grid; grid-template-columns: repeat(auto-fill, minmax(100px, 1fr)); gap: 1rem; margin-top: 1rem;">
  {Object.keys(FallbackIcons).map((noun) => (
    <div style="display: flex; flex-direction: column; align-items: center; padding: 1rem; background: var(--cn-surface-2); border-radius: 8px;">
      <CnIcon noun={noun} size="small" />
      <span style="font-size: 0.7rem; margin-top: 0.5rem; word-break: break-all;">{noun}</span>
    </div>
  ))}
</div>
