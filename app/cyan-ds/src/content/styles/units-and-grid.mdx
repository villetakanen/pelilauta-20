---
title: Units & Grid
description: Spacing, layout, and grid systems in the Cyan Design System.
order: 1
status: stable
---

# Units & Grid

The Cyan spacing system is built on a strict **8px baseline grid**. All spacing and sizing tokens derive from a single unit, `--cn-grid`, making the system rescalable by changing one value.

## Core Spacing

| Token | Derivation | Computed (16px root) |
|-------|-----------|---------------------|
| `--cn-grid` | `0.5rem` | 8px |
| `--cn-gap` | `grid × 2` | 16px |
| `--cn-line` | `grid × 3` | 24px |

<div style="display: flex; gap: var(--cn-gap); align-items: flex-end; padding: var(--cn-gap);">
  <div style="width: var(--cn-grid); height: var(--cn-grid); background: var(--color-primary);" title="--cn-grid (8px)"></div>
  <div style="width: var(--cn-gap); height: var(--cn-gap); background: var(--color-primary);" title="--cn-gap (16px)"></div>
  <div style="width: var(--cn-line); height: var(--cn-line); background: var(--color-primary);" title="--cn-line (24px)"></div>
</div>

## Responsive Sizing

Cyan v20 uses **container queries** instead of global media-query breakpoints. Components respond to their container's inline size, not the viewport.

- Layout containers declare `container-type: inline-size`
- Components use `@container` rules to adapt at locally meaningful thresholds

```css
/* Container setup */
.layout-region {
  container-type: inline-size;
}

/* Component responds to its container */
@container (min-width: 620px) {
  .card-grid {
    grid-template-columns: repeat(2, 1fr);
  }
}
```

## Border Radius

Border radius tokens provide consistent corner rounding, all derived from `--cn-grid`.

| Token | Multiplier | Computed |
|-------|-----------|---------|
| `--cn-border-radius-small` | `grid × 0.5` | 4px |
| `--cn-border-radius-medium` | `grid × 1` | 8px |
| `--cn-border-radius-large` | `grid × 2` | 16px |
| `--cn-border-radius-xl` | `grid × 4` | 32px |

<div style="display: flex; gap: var(--cn-gap); align-items: center; padding: var(--cn-gap);">
  <div style="width: 4rem; height: 4rem; background: var(--color-primary); border-radius: var(--cn-border-radius-small);" title="small (4px)"></div>
  <div style="width: 4rem; height: 4rem; background: var(--color-primary); border-radius: var(--cn-border-radius-medium);" title="medium (8px)"></div>
  <div style="width: 4rem; height: 4rem; background: var(--color-primary); border-radius: var(--cn-border-radius-large);" title="large (16px)"></div>
  <div style="width: 4rem; height: 4rem; background: var(--color-primary); border-radius: var(--cn-border-radius-xl);" title="xl (32px)"></div>
</div>
