---
feature: cn-hero
parent_spec: ../../spec.md
status: not-started
---

# Component: cn-hero

## Status

**Not started — placeholder.** This spec exists so that deferred tech debt in
other specs can link to a stable target. Tracked as the migration destination
for:

- `app/pelilauta/src/pages/login.astro` centered-hero layout (see
  `specs/pelilauta/auth/spec.md` §Out of Scope → "DS primitives").

## Blueprint

To be written when the primitive is prioritised. The first concrete need is a
centered vertical stack with a max-content-width lead paragraph and a primary
CTA slot — see the `login-page-hero` class in the current `login.astro` for
the shape.
